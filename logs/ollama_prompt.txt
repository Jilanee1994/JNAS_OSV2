Repair ONLY the listed generated files before writing them to disk.
Return ONLY replacement files using ===FILE:path=== blocks and one final ===END===.
No markdown. No explanations. No prose. No code fences.
Do not add new files. Do not regenerate the whole project.
Prefer standard-library code over third-party dependencies when possible.
If the dependency is truly required, return the same corrected file content.

Project name: BUILD_AGENT
Milestone: 1

Undeclared dependency issues:
- src/main.py imports undeclared dependency click

Current generated files:
===CURRENT_FILE:src/main.py===
import click
from dataclasses import dataclass

@dataclass
class Task:
    name: str
    action: callable

@dataclass
class Workflow:
    tasks: list[Task]

def cli():
    pass

@click.command()
@click.option('--project-goal', help='The goal of the project')
@click.option('--plan-execution', is_flag=True, help='Create an execution plan')
@click.option('--invoke-builder', is_flag=True, help='Invoke a builder step')
@click.option('--read-report', is_flag=True, help='Read or produce a build report')
@click.option('--handle-failure', is_flag=True, help='Handle failed steps with a retry path')
def main(project_goal, plan_execution, invoke_builder, read_report, handle_failure):
    if project_goal:
        print(f"Project goal: {project_goal}")
    if plan_execution:
        print("Creating execution plan...")
    if invoke_builder:
        print("Invoking builder step...")
    if read_report:
        print("Reading build report...")
    if handle_failure:
        print("Handling failed steps with retry path...")

if __name__ == '__main__':
    main()

