Repair ONLY the listed files to remove undeclared third-party imports when possible.
Return ONLY replacement files using ===FILE:path=== blocks and one final ===END===.
No markdown. No explanations. No prose. No code fences.
Do not add new files. Do not regenerate the whole project.
If the dependency is truly required, return the same corrected file content.

Project name: WEATHER_DASHBOARD
Milestone: 1

Undeclared dependency issues:
- src/job_search.py imports undeclared dependency requests
- src/job_search.py imports undeclared dependency beautifulsoup4

Current files:
===CURRENT_FILE:src/job_search.py===
import requests
from bs4 import BeautifulSoup

def get_weather_data(location):
    url = f"https://www.weather.com/weather/today/{location.lower().replace(' ', '')}"
    response = requests.get(url)
    
    if response.status_code != 200:
        return None
    
    soup = BeautifulSoup(response.text, 'lxml')
    try:
        current_temp = soup.find("span", {"data-testid": "TemperatureValue"}).text
        condition = soup.find("div", {"class": "CurrentConditions--tempValue--3a50n"}).text.strip()
        weather_data = f"Current Temperature in {location}: {current_temp}°F, Condition: {condition}"
    except Exception as e:
        return None
    
    return weather_data

