# Builder Agent V4 Deferred Improvements

- Add generated-project coverage measurement and include coverage in `BUILD_REPORT.md`.
- Add session-manager resume support for long-running Builder jobs.
- Add Tool Registry registration for Builder Agent V3 as a discoverable worker.
- Add richer per-provider configuration examples for Gemini, Groq, and OpenRouter.
- Add sandboxed runtime execution for generated applications that need side effects.
- Add optional protected-path approval policies through the Security module.
- Add generated dependency installation in an isolated virtual environment.
